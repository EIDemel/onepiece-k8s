---
apiVersion: v1
kind: Service
metadata:
  name: battle-service
  namespace: onepiece
  labels:
    app: battle-service
spec:
  selector:
    app: battle-service
  ports:
    - name: http
      port: 3005
      targetPort: 3005
      protocol: TCP
  type: ClusterIP
